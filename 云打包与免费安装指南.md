# iOS 用户端 · 云打包 + 免费安装操作指南（免费路线）

> 目标：不花一分钱，把「电子点名」iOS 版装到自己的 iPhone 上。
> 路线：**GitHub 仓库 → Codemagic 免费云构建（出未签名 ipa）→ Windows 用 Sideloadly 免费签名 → 装到 iPhone**。
> 注意：免费签名有效期 7 天，到期重签一次即可（数据不丢）。

---

## 第 1 步：把工程推送到 GitHub（一次性）

1. 注册 GitHub 账号：https://github.com/signup （免费）；
2. 新建一个**私有仓库**，名字随意，如 `checkin-ios`（不要勾选任何初始化文件）；
3. 在本工程目录执行（把下面的 `你的用户名` 和仓库名换成自己的）：

```
cd "C:\Users\33352\AppData\Local\Doubao\User Data\Default\.doubao\agent_mode\workspace\checkin_system\checkin_apk"
git init
git add -A
git commit -m "电子点名用户端 v6.0（iOS 云打包就绪）"
git branch -M main
git remote add origin https://github.com/你的用户名/checkin-ios.git
git push -u origin main
```

> 首次 push 会要求登录 GitHub（浏览器弹窗授权即可）。
> 仓库里不含 node_modules / platforms / data（已用 .gitignore 排除），构建时自动下载。

---

## 第 2 步：Codemagic 免费云构建（出 ipa）

1. 打开 https://codemagic.io → 注册（可用 GitHub 账号一键登录，免费）；
2. 登录后点 **Add application** → 选择 GitHub → 授权并选择刚建的 `checkin-ios` 仓库；
3. 进入应用页，右侧选择 workflow：**iOS 用户端 - 未签名 IPA** → 点 **Start new build**；
   - 免费计划每月 500 分钟 macOS 构建，本流程约 10–20 分钟，完全够用；
4. 等待构建（页面会实时滚动日志）。若中途失败，把红色报错文字复制回来，我可以帮你定位；
5. 构建成功后，页面底部 **Artifacts** 里下载：
   **`unsigned-checkin-user.ipa`**（约 50–100MB）。

---

## 第 3 步：Windows 上免费签名并安装（Sideloadly）

### 准备
- iPhone 上登录一个**免费 Apple ID**（appleid.apple.com 免费注册；若 iPhone 已登录 Apple ID 直接用）；
- Windows 电脑装 **iTunes**（或仅 Apple Devices）用于驱动，并装 **Sideloadly**（免费开源）：
  https://sideloadly.io  （下载 Windows 版，安装时如被杀软拦截属正常，添加信任）

### 签名安装
1. iPhone 连数据线到电脑，解锁并「信任此电脑」；
2. 打开 Sideloadly：
   - **IPA** 一栏：选择下载的 `unsigned-checkin-user.ipa`；
   - **Apple ID** 一栏：填你的免费 Apple ID 与密码；
   - **APP ID** 一栏：保持默认（`com.checkin.user`，若提示被占用则改为如 `com.checkin.user1`，首次安装后不要随意改）；
   - 点 **Start** → 等待签名安装完成（首次约 1–3 分钟）；
3. 手机上：设置 → 通用 → **VPN 与设备管理** → 找到你的 Apple ID → 点「**信任**」；
4. 回到桌面，打开「电子点名」App。

> 若提示「无法安装/未受信任的开发者」，重复第 3 步信任操作即可。
> 打卡数据存在 App 本地（离线队列）并同步到管理端，重签不丢数据。

---

## 第 4 步：7 天到期续期

免费签名有效期 7 天，到期后 App 打开即闪退。续期方法（二选一）：
- **手动**：把 iPhone 连回电脑，Sideloadly 再次 Start 重签安装（数据保留）；
- **自动**（推荐）：安装 **AltStore** + Windows 版 AltServer 常驻后台，且 iPhone 与电脑在同一 WiFi 并开启「无线局域网同步」时，到期前会自动重签，无需插线。

---

## 常见问题

| 问题 | 处理 |
|---|---|
| 构建失败：`pod install` 报错 | 把报错日志发我；多数是插件 iOS 版本兼容，可换插件或锁版本 |
| 构建失败：找不到「电子点名.app」 | 通常是 build 配置/产物路径变化，发我日志定位 |
| 签名时报 `Apple ID 密码错误` | 若开了双重认证，需到 appleid.apple.com 生成「应用专用密码」填到 Sideloadly |
| 装好后 App 打不开（闪退） | ① 检查「VPN 与设备管理」里是否已信任；② 确认未到 7 天；③ 换一条原装数据线重签 |
| 装好后连不上管理端 | ① iPhone 与电脑同一 WiFi；② 管理端 EXE 防火墙 8090 已放行；③ 在 App 里重新设置管理端地址 |
| 扫码按钮提示「当前环境不支持」 | iOS 的扫码插件兼容问题，先用「手动输入地址」连接；后续可换 barcodescanner 插件修复 |
| 想长期不过期、正式分发 | 免费路线做不到；单位长期用需企业证书（$299/年）或 TestFlight（$99/年） |

---

## 相关文件说明（已就绪）

| 文件 | 作用 |
|---|---|
| `codemagic.yaml` | Codemagic 构建配置（未签名 ipa 全流程） |
| `.gitignore` | 提交排除项（依赖/产物/数据不推仓库） |
| `config.xml` | 已含 iOS 平台段：ATS 局域网放行、相机/相册/定位权限、图标、竖屏 |
| `package.json` | 已加 `cordova-ios` 依赖与 `build:ios` 脚本 |
