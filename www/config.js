/* ============================================================
 * 打卡系统 v6.0 · 全局配置文件（config.js）
 *
 * amapKey：高德地图 Web 服务 Key（逆地理编码）。
 *   - 申请地址：https://console.amap.com/  （Web 服务类 Key）
 *   - 填入后系统在线打卡/地址回填优先调用高德，带 extensions=all
 *     返回「省+市+区县+街道+门牌号」完整地址；
 *   - 留空或调用失败时，自动降级为免费备用地址服务，不影响打卡；
 *   - 注意：高德 Key 校验调用来源 IP 白名单，电脑更换网络/地点后
 *     需在高德控制台更新白名单（错误码 10005 即白名单不匹配）。
 *
 * 管理端「系统设置」中也可直接修改本 Key（会保存到服务端数据，
 * 用户端连接后自动下发），本文件为离线默认值。
 * ============================================================ */
window.CHECKIN_CONFIG = {
  amapKey: ''
};
